# OpsCopilot M2 执行排期（W9~W11）

- **制定日期**：2026-09-11 · 基线：M1 出口收口（方案 B，`docs/M1出口验收报告-2026-09-11.md`）· commit `d5ce126`
- **M2 目标**：降噪从"影子"转正为"真拦截 + 真通知"，事件域补全为运维主工作面，RCA 从骨架变可用——交付"告警进来 → 自动降噪 → 建单/通知 → 辅助定位 → 人工闭环"的完整价值链。
- **前置条件（已满足）**：W6-3 评估 100% 达标（D13=C 的"先评估"完成）；双链路事件域一二期已落地；控制台事件页已上线。
- **人力估算**：净 ~10-12 人日 + 缓冲，日历 3 周（与 M1 节奏一致：一周一个主战场）。

---

## W9（M2 第 1 周）：影子转正 + 通知闭环 —— M2 的 P0

> M1 出口第一个可交付闭环：降噪**真的开始省事**。

**进度（2026-09-11）**：W9-1 ✅ `b27fbc8`（shadow/enforce 开关 + 闸门接线 + 计数持久化，ADR-011）· W9-2 ✅ `59ac7ec`（三形态渠道 + 热配置 + 控制台设置页）· W9-3 ✅ `909d84b`（严重级路由 + 未 ack 超时升级一次；迁移 000013/000014）· W9-4 ✅ `7d1d0af`（`/metrics` 延迟分位 + P95 实测 0.54s；顺带修掉 Prometheus 告警发射时刻恒空的前置缺陷，见 `docs/W9-4-E2E延迟打点与P95实测.md`）· W9-5 ✅ `892a7cf`+`e4c15f4`+`6de037b`（第八轮残留 3 项：变更库 `PruneBefore` 接线 + 容量日志 / 计数原子化 + Redis·HTTP 超时 / 审计 List 错误透传）· W9-6 ✅ `7d597fd`（容量模型三级估算，回写架构方案 v1.3 第 7 章）· **W9 出口 ✅ `06c472e`**（enforce 端到端演练全绿：拦截 15/21 + 建单 7 + 双渠道通知 6+6 + 评估 9/9 段 100%，见 `docs/W9出口演练记录-enforce端到端.md`）。**W9 全部完成，M2-M1 里程碑达成；下一周 W10。**

| # | 任务 | 产出 | 验收 | 估时 |
|---|------|------|------|------|
| W9-1 | **转正开关与灰度**：`OPS_NOISE_MODE=shadow\|enforce`（默认 shadow 不变行为）；enforce 下 Gate 真拦截 + 拦截计数持久化（消 R6-6） | ADR-011（转正策略：默认 shadow、灰度切换、可一键回退） | 切 enforce 后注入器评估仍 100%，且拦截决策可回放 | 1.5d |
| W9-2 | **通知渠道与抑制执行**：WouldSuppress→真通知放行判定；渠道实现（webhook 通用 + 飞书/企微 IM webhook），渠道配置入 DB + 控制台 settings 区 | notify 渠道落库迁移 + 发送 worker | 假渠道端到端：簇 new-incident → 通知送达；dedup/merge 不重复通知 | 2d |
| W9-3 | **值班与升级规则（最小版）**：通知路由规则（severity → 渠道），超时未 ack 重发一次；不做复杂排班 | 升级规则表 + 重发逻辑 | 未 ack 事件 15min 后二次通知 | 1d |
| W9-4 | **E2E 延迟专项打点**（M1 遗留）：alert fired→verdict→通知全链路耗时指标 + `/metrics` 暴露 | 打点字段 + P95 报表 | 补齐 M1 出口标准 3 的直接证据（P95<30s 实测） | 0.5d |
| W9-5 | **第八轮审核剩余 3 小项顺手清**：PruneBefore 周期接线；NoiseEngine 计数原子化 + http.Server 超时；PGAuditLog.List 错误透传 | 3 提交 | gofmt/vet/全量测试绿 | 0.5d |
| W9-6 | **容量模型三级估算**（W7 前置①，纯文档可并行）：告警量/存储增长/连接数三级估算，回写架构文档 | 容量模型文档 | 数值有压测与评估数据支撑 | 0.5d |

**W9 出口**： enforce 模式下端到端演练通过（注入器 4 场景 → 降噪拦截 → 建单 → 通知送达）。✅ **已达成（2026-09-11，`06c472e`）**：拦截 15/21 + 建单 7（origin=prometheus，指纹幂等）+ 双渠道通知 6+6 + 评估 9/9 段 100% + 延迟 P95 0.48s；证据见 `docs/W9出口演练记录-enforce端到端.md`。

---

## W10（M2 第 2 周）：事件域补全 —— 运维主工作面

**进度（2026-09-12）**：优化方案 **12/12 全部收口** + **二期池三波 8 项全部落地**（llm-gateway 转正 conclude #3、自动触发 #4、findings 上限 #6、RCA 评测集 #8 等）+ **RCA 复盘会话 S1+S2**（#7，`docs/设计-sessionstore消费方与接线.md` 蓝图兑现）+ **W10-6 簇→事件生产自动挂簇 ✅** + **W10-1 F-03 事件混合时间线 ✅**（`ecedc47`+`db4dd57`，本节"W10-1 落地注记"）。逐项提交号与验收细节**不在此逐条复述**：见 `docs/OpsCopilot项目优化方案-2026-09-11.md` 进度行与二期池排序、`docs/配置清单-OpsEnv.md`（OPS_RCA_*/OPS_LLM_*/OPS_SESSION/OPS_AUTOATTACH 键表）；本期推进的 commit 区间 `6f433b9..db4dd57`（含 W10-6/W10-1）。

| # | 任务 | 产出 | 验收 | 估时 |
|---|------|------|------|------|
| W10-1 | ✅ `ecedc47`+`db4dd57`（2026-09-12，口径见本节"W10-1 落地注记"）**F-03 事件混合时间线**：告警进出 + 变更记录 + 人工处置动作按时间归并（复用 alert_event/change_record/audit 三源），REST + 控制台详情区 | `GET /incidents/{id}/timeline` | 三类事件混排、时间正确、分页 | 1.5d |
| W10-2 | **F-04 SLA 时钟**：事件级目标时长 + 进度条 + 超时标记（M2 只记录与展示、不自动升级，对齐清单口径） | SLA 字段迁移 + 前端 sla-bar | 事件页可见 SLA 状态 | 1d |
| W10-3 | **F-07 运维 KPI**：MTTA/MTTR/平均闭环，仪表盘 KPI 行（数据源=时间线） | KPI 聚合端点 + 控制台 | 与手工按时间线计算一致 | 1d |
| W10-4 | **autoCreate 转正决策落地**（D13 收尾）：外部告警自动建单默认值评审——限流/代际/通知均就绪后，给出"自动建单 on"的开关与灰度方案 | ADR-012 + 开关 | 决策留痕，若 on 则压测回归 | 0.5d |
| W10-5 | **多适配器第二期残留**：pull 类第二适配器（如 Azure Monitor）按需插入；无真实源则保留接口与测试桩 | 适配器骨架 | CI 测试桩通过 | 0.5d（可让位） |
| W10-6 | ✅ `3d7f44c`（2026-09-12，挂点/开关依赖/共簇口径见本节末"W10-6 落地注记"）**簇→事件生产自动挂簇**（F-02 接线，事件域补全时并入）：成簇达建单阈值自动建单并 `AttachCluster` 挂簇。现状：`audit.go:37` `AuditAttachCluster` 注释自证生产代码无调用路径；rca_eval 按 AttachCluster SQL 语义直写 `incident_cluster` 桥接属临时形态（评测报告 §1/§5 暴露项），转正前必须有产品化挂点（候选：影子判决 new-incident 联动建单挂簇）——**已落地为候选挂点** | 自动挂簇路径 + attach_cluster 审计落库 + 测试 | 达建单阈值即自动 attach（无需评测桥接）；`AttachCluster` 幂等语义不变（重复挂同簇同单不产生副本、不覆盖首挂）；一簇一事件约束（`idx_incident_cluster_unique`）下多指纹共簇仅一单有故障域——其余事件的处置口径须给出明确说明（文档 + 评测报告 §5 同步收口） | 0.5d（并行不占关键路径） |

**W10 出口**：控制台事件页成为"看板 + 工单"双职能：时间线/SLA/KPI 全部真实数据。

> **W10-6 ✅ 落地注记（2026-09-12）**：挂点 = **enforce 判决 new-incident 出口**
> （`cmd/opscopilot/noise_process.go` `autoAttachClusters`，开关 `OPS_AUTOATTACH`
> 默认 off，on 须 enforce——`config.Validate` fail-fast；与 `OPS_INCIDENT_AUTOCREATE`
> 无硬依赖，建单幂等键 `origin=prometheus + source_ref=promFingerprint(labels)`
> 与链路 A 同源，两路同开天然收敛一单）。挂簇动作落 `attach_cluster` 审计
> （actor=`system:autoattach`，`AuditAttachCluster` 自此非死代码）。
> **多指纹共簇处置口径（验收要求的明确说明）**：只在簇**创建**（new-incident 判决）
> 时建单挂簇——**首单持故障域**；后续共簇指纹的判决是 cluster-merge，不再触发挂簇，
> 其自身事件（链路 A 所建）**无域**，处置口径 = **人工 `MergeInto` 归并到首单**
> （L2 只提示不自动级联）。唯一索引竞态（重放/双实例）下 `AttachCluster` 返回可
> `errors.Is(incident.ErrClusterTaken)` 的冲突 → **跳过并计 conflict，不报错不级联，
> 绝不覆盖首挂**；三出口计数 `opscopilot_autoattach_total{outcome=attached|conflict|skipped}`。
> **评测桥接收口**：`run_rca_eval.sh` 评测接线改 `OPS_NOISE_MODE=enforce +
> OPS_AUTOATTACH=on`，`tools/rca_eval` ③ 步改为"生产挂簇路径优先验证、桥接直写
> 仅未开开关时回退（note 显式标注）"；证据版重跑读数见 `docs/reviews/rca-eval-2026-09-12-2332/`
> （4/4 + guards 3/3，四单元 note 全部"挂簇走生产自动路径"、零回退桥接），报告 §5 同步收口。

> **W10-1 ✅ 落地注记（2026-09-12）**：`cmd/opscopilot/rest_timeline.go`（cmd 层
> 三源汇合，internal 纪律不破）→ `GET /api/v1/incidents/{id}/timeline`。
> **DTO** `{ts, kind: alert_in|alert_out|change|action, source_id, summary,
> severity|confidence}`：告警进出按 alert_event 影子判决持久化字段分类
> （`would_suppress∨would_converge ⇒ alert_out` 降噪出场，否则 `alert_in`——
> 不猜 firing/resolved，表里没有的语义不编）；变更按故障域（簇 NodeKeys 并集，
> RCA faultDomain 同源）×事件生命周期窗口（`[created−RCA 回看窗, resolved|now]`，
> 变更常在故障**前**）关联，无域 ⇒ 无可归因变更（不把全库变更泼上单页时间线）；
> 处置 = incident_audit 全动作（attach_cluster/rca/transition 同列）。
> **同刻稳定序** `change < action < alert_in < alert_out`，再 source_id 升序
> （因果阅读序；三元组全序 ⇒ 分页决定性）。**分页** offset 不透明游标
> （风格与 /incidents 的 next_cursor 一致：空=末尾、坏游标 400；limit 默认 200
> 上限 1000 复用 incident 常量）——排序时间**升序** + 新事件只追加尾部 ⇒
> 翻页不重不漏（测试断言）。**partial 降级**：源未接线（无 DSN ⇒ 告警源缺席、
> 降噪 off ⇒ 变更无法归因、无 audit）/查询失败/单源截断（2000 行上限）→ 该源进
> `missing{原因}` + `partial=true`，**200 返回可用源归并**（汇合视图不因单源
> 塌方整页 5xx；专用端点如 /audit 的 D5"故障如实 500"口径不变）；incident
> 不存在 404、事件域未接线 503。前端落 `web/` 事件详情区（console.html 冻结）：
> kind 徽标 + 时间升序 + 游标"加载更多" + 降级横幅。**证据**：表驱动假源混排/
> 同刻序/翻页不重不漏/坏游标/降级 + 无 DB handler 降级端到端（`rest_timeline_test.go`）
> 与 `OPS_TEST_PG_DSN` 真库端到端（W10-6 生产路径建单挂簇 → 注入变更 → 人工 ack
> → 三类齐 + 时间单调 + limit=1 全量翻验 + 404，`rest_timeline_e2e_pg_test.go`）。
> **下游解锁**：W10-3 KPI（MTTA/MTTR）数据源即本端点 items。

---

## W11（M2 第 3 周）：RCA + 体验增强 + 交付收尾

| # | 任务 | 产出 | 验收 | 估时 |
|---|------|------|------|------|
| W11-1 | **F-05 RCA 流水线填充**：六步从占位变实现——as_of 拓扑（T0）+ 窗口变更 + 簇证据 → CausalSubgraph 硬门禁 → 候选根因排序 | rca 六步真实实现 + 测试 | 注入器 4 场景根因命中已知答案 ≥3/4 | 2d |
| W11-2 | **F-06 根因标注**：结论挂根因节点 + 置信度条（ADR-007 同源），控制台拓扑高亮根因链路 | RCA 端点 + 前端 | 事件详情可见根因与证据链 | 1d |
| W11-3 | **F-09 拓扑画布升级**：环形图 → topo-canvas 形态（节点卡/悬浮 tip/边高亮/根因链路高亮） | 前端改造 | 双分辨率截图验证 | 1d |
| W11-4 | **F-12 Runbook（记录版）**：事件挂载处置手册 + 执行记录展示（不自动执行） | runbook 表 + 端点 + 前端 | 手册挂载/查看闭环 | 1d |
| W11-5 | **F-13 AI 抽屉（只读版）**：事件/RCA 追问，LLM 走 ADR-003 单出口；无 key 时优雅降级隐藏 | 抽屉 UI + 单出口接线 | 只读问答，无执行类工具 | 1.5d（可让位） |
| W11-6 | **W7 前置②③：`opscopilot upgrade`（跨版本拒启 + 升级前自动备份）+ 备份恢复演练** | upgrade 子命令 + 演练记录 | 跨版本拒启生效；RPO<5min/RTO<1h 演练通过 | 1.5d |

**W11 出口（= M2 出口）**：全量测试绿 + 第十轮全局审核 + M2 出口评审纪要。

---

## 并行不占关键路径

- F-10 告警中心统计视图（P3，前端聚合，随时插入）；
- 多实例 per-item 限流（M2 决策项，单 worker 语义下不触发，留观察）；
- 读路径鉴权 + 游标 HMAC（依赖部署形态决策，M3 候选）。

## 关键依赖与风险

```
W9-1 转正开关 ──→ W9-2 通知 ──→ W9-3 升级规则
                    └────────→ W10-4 autoCreate（依赖通知闭环可用）
W10-1 时间线 ──→ W10-3 KPI；W10-2 SLA 独立
W11-1 RCA ──→ W11-2 标注 ──→ W11-3 画布高亮
W11-6 upgrade ──→ 私有化交付（M2 内必须闭环，方案 B 承诺）
```

| 风险 | 缓解 |
|------|------|
| enforce 误拦截（评估环境≠生产分布） | 默认 shadow、按租户灰度、一键回退（W9-1 ADR 硬性要求）；enforce 首周 shadow 双跑对比 |
| 通知风暴（簇分裂/代际复发） | 复用建单限流 + 渠道级聚合计数；每渠道每簇冷却窗 |
| RCA 算法命中率不达标 | 已知答案只有 4 场景，命中率指标先按 ≥75% 定级，不达标降级"证据展示"而非"根因断言" |
| 代理 502 推送中断（常态化） | 本地提交纪律不变，网络恢复即推；关键节点（W 周出口）必须确认远端同步 |

## 里程碑

- **M2-M1（W9 末）**：转正上线，通知闭环端到端通过。
- **M2-M2（W10 末）**：事件域主工作面成型（时间线/SLA/KPI）。
- **M2-M3（W11 末）**：RCA 可用 + W7 前置清零 + M2 出口评审。
