# M1 第 0 周任务审计报告

> 审计日期：2026-09-08 ｜ 审计对象：`opscopilot/` 全部交付物 + ADR-006 + 架构方案 v1.3 §6 实施步骤
> 审计方法：逐文件比对原始要求 + 实际运行验证（build/vet/test/边界检查/冒烟/配置守护三例）+ 交叉一致性核对
> 收尾更新：2026-09-09 —— W1 基座全部就位，O4/O9/O12/O13/O14/O15/O16 关闭，推送 GitHub 并同步 origin/main

---

## 一、审查范围

| 范围 | 内容 |
|---|---|
| 纳入 | T1 许可落档、T2 go-plugin 冒烟、P1-1 会话状态、P1-2 调用纪律守护（四条并行任务全部子项）；v1.3 §6 步骤 1-3；遗留清单中标记"立即"的 T3 |
| 不纳入 | v1.3 §6 步骤 4-7（STS 预研、直通环形缓冲、季度演练、压测回写——排期为 M1 内/M1 末，未到期，属正常未开始） |
| 验证手段 | `go build/vet/test ./...`、`scripts/check_module_boundaries.py`、冒烟实测、配置守护三例运行、文件与文档逐条比对 |

---

## 二、逐项核对结果

### T1 TimescaleDB 许可落档
| 子项 | 状态 | 说明 |
|---|---|---|
| 合规结论三条 | ✅ | SaaS 增值服务 ✅ / 单机分发未修改二进制 ✅ / 禁止修改后分发 ⚠️ 已落档 |
| 降级链（自建→RDS→Tiger Cloud） | ✅ | 含触发条件与代价 |
| 关键外部依赖清单 | ✅ | 6 组件 × 版本/许可/特性/部署约束 |
| 法务程序性确认 | ⏸ | ADR 已标注待办（O8，W8 前并行），无阻塞风险 |

### T2 go-plugin 冒烟
| 子项 | 状态 | 说明 |
|---|---|---|
| Windows 实测 | ✅ | windows/amd64 三项 PASS（初测 windows/386，收尾重装 amd64 复测），`network=tcp` 回环确认 |
| **Linux 实测** | ✅ | CI Run #3 三平台全绿（build / windows / linux 冒烟均 PASS），原"仅跑 Windows"缺口已通过 CI 自动覆盖（O4 关闭） |
| 结论落档 ADR-006 | ✅ | 已写入，含 P1-3 降级为可选优化的连带结论 |
| 冒烟进入 CI | ✅ | 已修：拆出插件构建步骤（Windows + 新增 Linux job）、新增 setup-python 3.12 |

### P1-1 会话状态落位
| 子项 | 状态 | 说明 |
|---|---|---|
| 双实例配置强制（启动期） | ✅ | 未配置/同地址/角色互换均拒启，已实测 |
| 会话绑定持久实例 panic 守卫 | ✅ | 单元测试 2 例覆盖 |
| config 单元测试 | ✅ | 3 例 |
| **真实 Redis 集成测试** | ❌ | 读写路径未验证（O10，排队到 W5） |
| **main.go 实际装配** | ⏸ | 仍为 TODO（M1 主体范围内，可接受） |

### P1-2 调用纪律守护
| 子项 | 状态 | 说明 |
|---|---|---|
| 静态检查脚本 | ✅ | 三连验证（违例抓取/合法放行/干净通过） |
| 契约边界（contracts 白名单） | ✅ | 规则已实现并验证 |
| 进程内 gRPC 抽象（transport） | ✅ | 已补进程内 gRPC 往返测试（health 探针）+ 单连接阻塞行为固化（O9 关闭） |
| **检查脚本自身回归测试** | ✅ | `--selftest` 5 用例全过，纳入 CI（O16 关闭） |
| CI 集成 | ✅ | 已修：setup-python + 显式 3.12（`python3` 可用） |

---

## 三、遗漏项清单

| # | 对应任务 | 缺失描述 | 影响 | 补救措施 | 优先级 |
|---|---|---|---|---|---|
| O1 | T2 / CI | **CI Windows 冒烟缺 `go build -o bin/plugin.exe` 步骤**，宿主拉起插件必失败 | 流水线红，冒烟形同虚设 | ✅**已修**：拆出构建步骤 | P0 |
| O2 | P1-2 / CI | CI 用 `python` 调用检查脚本，ubuntu runner 默认只有 `python3` | 边界检查 job 失败 | ✅**已修**：新增 setup-python 3.12 | P0 |
| O3 | 工程规范 | 缺 `.gitignore`，`bin/plugin.exe`（18MB）会被提交，凭证配置无入库防护 | 仓库污染 + 密钥泄露风险 | ✅**已修**：补 `.gitignore`（含 bin/、*.exe、credentials.yaml） | P0 |
| O4 | T2 | 未按原始要求跑 Linux 冒烟 | 结论只覆盖 Windows；Linux 是私有化主要目标环境 | ✅ **已关闭**：CI Run #3 三平台全绿（build / windows / linux 冒烟均 PASS），Linux 由 CI 自动覆盖 | P1 |
| O5 | v1.3 §5.2 | **ADR-001~005 未补齐**（v1.3 要求"立即执行"） | 决策无留痕，是 O1 类冲突复发的根因 | ✅**已修**：ADR-001~005 补齐 + `docs/adr/README.md` 索引（含写新 ADR 的检查清单） | P1 |
| O6 | 遗留清单 | **T3 最小资源规格**标记"立即"但未启动（初始值 4C8G 未实测） | 客户装机失败/卡顿风险 | 装 Docker/Redis 实测一次，写入安装预检脚本 | P1 |
| O7 | v1.3 §6 步骤 3 | **数据库运维 owner + Patroni 拓扑设计**未开始（M1 第 0-1 周） | 高可用设计缺责任人，易停留在纸面 | 指定 owner（角色即可）+ 输出 Patroni 拓扑与监控接入方案文档 | P1 |
| O8 | T1 | 法务程序性确认未完成 | 合规结论缺少法务背书 | 走法务复核流程，结论回写 ADR-006 | P2 |
| O9 | P1-2 | transport 模块零测试；单连接假设（singleListener 只 Accept 一次）未验证多连接场景 | 拆分形态时可能踩坑 | ✅ **已关闭**：补进程内 gRPC 往返测试；明确"一个 ClientConn 一个 Pipe"约束并写注释 | P1 |
| O10 | P1-1 | sessionstore 无真实 Redis 集成测试 | 读写路径与 TTL 行为未验证 | 加 miniredis 或 testcontainers 集成测试（M1 主体期，W5） | P2 |
| O11 | 工程规范 | 目录未 `git init`，无本地仓库 | CI 无法触发、无版本历史 | ✅**已修**：`git init` + 首次提交（commit `0549778`；审计初稿记为 `6d9d0c7`，`filter-branch` 改写作者后哈希变更）；未关联远端，提交身份为占位（`cai <cai@localhost>`），推送前需修正 | P1 |
| O12 | 文档一致性 | README 标题"四条并行任务状态：全部完成" vs T1 法务待办、T2 Linux 未跑 | 表述略强于事实，易误导评审 | ✅ **已关闭**：README 已刷新，标注 64 位 Go / 已推送 / 契约已生成 / 已知限制 | P2 |
| O13 | contracts | `scripts/gen.sh`（protobuf 契约生成）代码注释标"待建"，无排期 | 跨模块契约落地时缺工具 | ✅ **已关闭**：`scripts/gen.sh` 已生成 `internal/contracts/pb/topology.pb.go`(603) / `topology_grpc.pb.go`(169)；protoc 已装 | P2 |
| O14 | 文档一致性 | README 目录树含 `pkg/`，实际不存在 | 文档与代码不符 | ✅ **已关闭**：`internal/contracts` / `pkg` 目录已就位，README 目录树与代码一致 | P3 |
| O15 | 工程规范 | 无 LICENSE 文件 | 商业化/开源边界不清 | ✅ **已关闭**：已补专有软件授权协议（中文 + 英文双语，copyright ZainCai 2026），commit `eef2b49` | P3 |
| O16 | P1-2 | 边界检查脚本无自测（fixtures） | 规则改动后无回归保护 | ✅ **已关闭**：加 `scripts/testdata/` 违例样例 + CI 自测用例，`--selftest` 5 用例全过 | P2 |

---

## 四、本次顺手修复（3 项）

1. **`.github/workflows/ci.yml`**：补齐插件构建步骤（Windows + 新增 Linux job）、新增 `setup-python` 并显式指定 3.12；go-version 对齐 go.mod 为 `1.23`；
2. **`.gitignore`**：新增，排除 `bin/`、`*.exe`、`.env`、`credentials.yaml` 等——后者是架构"凭证绝不入库"要求的落地；
3. **LICENSE**：新增专有软件授权协议（中文 + 英文双语），明确商业化/开源边界。

---

## 五、结论

- **四条并行任务主体完成、可验证**：编译/vet/测试/边界检查/冒烟/配置守护全部实测通过，无虚假完成项；
- **发现 16 项遗漏，已关闭 12 项**（O1/O2/O3/O5/O11/O9/O13/O14/O16/O4/O12/O15），剩余 4 项（O6/O7/O8/O10）按 P1/P2/P3 排期；
- **剩余最需要优先处理**：O6（最小资源规格）与 O7（数据库 owner + Patroni 拓扑）——两者都会把后续设计卡在纸面上；O4 已由 CI Run #3 三平台全绿关闭；
- **无重大架构性遗漏**：所有遗漏均为工程落地与流程配套层面，架构方案 v1.3 本身无需再修订。

---

## 六、后续处理记录（更新至 2026-09-09）

### 已完成（追加）
1. **O5**：补写 ADR-001（事件骨干不用 Kafka）、ADR-002（算子前置）、ADR-003（LLM 单出口）、ADR-004（Redis 双实例）、ADR-005（审计分离），并新增 `docs/adr/README.md` 索引（含"写新 ADR 的三条检查清单"：影响已有 ADR / 引入外部依赖 / 记录否决理由）。**ADR-001~006 现已齐全**，每条末尾的"交叉检查提醒"记录了耦合项，正是防 C17 类冲突复发的具体手段。
2. **O11**：`git init` + 首次提交（commit `0549778`；审计初稿记为 `6d9d0c7`，`filter-branch` 改写作者后哈希变更）。`.gitignore` 生效，`bin/plugin.exe` 未入库。
3. 追加工程配套：`.gitattributes`（统一 LF，避免跨平台全量 diff）。
4. **O9**：transport 模块补进程内 gRPC 往返测试（health 探针）+ 单连接阻塞行为固化。
5. **O13**：`scripts/gen.sh` 已生成 `internal/contracts/pb/topology.pb.go`(603) / `topology_grpc.pb.go`(169)；protoc 已装。
6. **O14**：`internal/contracts` / `pkg` 目录已就位，README 目录树与代码一致。
7. **O16**：边界检查脚本 `--selftest` 5 用例全过，已纳入 CI。
8. **O4**：CI Run #3 三平台全绿（build / windows / linux 冒烟均 PASS），Linux 冒烟由 CI 自动覆盖。
9. **O12**：README 已刷新（64 位 Go / 已推送 / 契约已生成 / 已知限制）。
10. **O15**：已补专有软件授权协议（中文 + 英文双语，copyright ZainCai 2026），commit `eef2b49`。
11. **推送与远端**：已建 GitHub `ZainCai/OpsCopilot` 并 push，`main` 与 `origin/main` 同步（ahead 0）；提交身份 `ZainCai` 已 `filter-branch` 对齐。

### 剩余待办（4 项）
- **O6** 最小资源规格（W7）：Docker/Redis 实测 4C8G 预检，写入安装预检脚本。
- **O7** 数据库运维 owner + Patroni 拓扑（W8）：指定 owner + 输出 Patroni 拓扑与监控接入方案。
- **O8** 法务确认（W8 前并行）：TimescaleDB TSL 商业化口径，结论回写 ADR-006。
- **O10** sessionstore 集成测试（W5）：miniredis / testcontainers 集成测试。
