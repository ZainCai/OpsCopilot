# OpsCopilot M1 后续工作计划（2026-09-09 制定）

**当前坐标**：W1~W3 全部收官（环境底座 / 连接器框架 / 语义模型 v1，真实 Prometheus 全链路 11 节点入图验证通过）；三轮代码复审 36 项问题全部闭环（HEAD = `0745d29`，已推送）；部署链路（Dockerfile/compose）修复后**尚未端到端实测**。

**M1 唯一智能目标不变**：影子模式降噪准确率 >85%。RCA/llm-gateway/IM 推送等全部留在 M2，不要被带偏。

---

## 一、优先级总排序

| 优先级 | 工作块 | 理由 |
|---|---|---|
| **P0** | W4 降噪引擎（影子模式） | M1 出口标准的直接来源；W3 已留好接入点，晚开工=压缩 W6 调优窗口 |
| **P0** | 接线清单（代码内预留 API 激活） | Host 注册、告警消费、credential 清扫——W4 开工的前置动作，半天量 |
| **P1** | W5 告警簇 API + 最小控制台 | 影子数据的可视化出口；依赖 W4 的簇状态机 |
| **P1** | gRPC 服务接线（GetTopology/GetRecentChanges） | 契约与映射全部就绪（as_of/ToProto），控制台拓扑查询的直接供给方 |
| **P2** | W6 影子评估与调优 | 需要 W4 产物跑满 1 周数据，本质是日历时间，早开工早收敛 |
| **P2** | W7 私有化打包 | 不依赖降噪质量，可与 W6 并行 |
| **P3** | W8 出口验收 | 汇总性质，依赖全部前序 |

---

## 二、分阶段目标与任务拆分

### 阶段 1：W4 降噪引擎（影子模式）—— 预计 5~6 人日

**目标**：告警从"只计数"变成"可聚类的簇"，影子模式跑起来（只标注不收敛）。

| # | 任务 | 说明 | 工作量 | 里程碑/验收 |
|---|---|---|---|---|
| 1.1 | **接线清单激活**（前置）✅ 2026-09-10 | ① main 注册 prometheus(+azure 可选) 进 Host 并启动 `Host.Run`；② credential 周期清扫挂载（`SweepExpired`，C9 预留）；③ compose 已就绪，本机实测一次 `docker compose up` 端到端（X1/X2 修复后未验证）。**完成记录（commit 4ad8817）：①②落地 `cmd/connectors.go`+`main.go`；③ Docker Desktop 本机不可用，改用二进制冒烟：假 Prometheus → 健康/采集(1 alert)/发现(1 node) → webhook 200+幂等+幽灵节点 422 全通过** | 0.5 天 | main 启动后 healthz 200 + 真实 demo Prometheus 节点持续入图 |
| 1.2 | 告警指纹与去重 ✅ 2026-09-10 | Alert → 指纹（labels 规范化哈希）；`alert_event` 表已建好 hypertable（000003 已入库）。**完成记录（commit 4ad8817）：`internal/noise` 纯库零依赖——Fingerprint（key 字典序+\x1f 连接，FNV-1a 64）+ Dedup（固定窗口锚点制，空指纹/零窗口放行"宁漏勿杀"，Allow 周期惰性清扫+Sweep+Len），9 项测试锁定** | 1 天 | 指纹单测 + 重复告警去重 |
| 1.3 | 时间窗聚类 + 拓扑邻近聚合 ✅ 2026-09-10 | 簇状态机（open/acked/resolved，ADR-001）；拓扑邻近用 CausalSubgraph 判定"同一故障域"。**完成记录（commit da1dda7）：`internal/noise/cluster.go`——并入规则指纹优先+故障域（FaultDomainFunc 注入，cmd 装配层接 topology 实现）；状态机含 acked 幂等/resolved 终态/窗口超时自动 resolve；簇 key=c:<首指纹>@<窗口桶号>；决定性契约（候选取 LastSeen 最新、同序重放同结果）14 项测试锁定。测试中抓到并修掉真缺陷：域聚合曾遍历 resolved 簇。** | 2 天 | 单元/集成测试；聚类行为锁定 |
| 1.4 | 影子模式标注输出 ✅ 2026-09-10 | 只标注"应收敛"不实际收敛；开关可配。**完成记录（commit 1dcaf59）：`internal/noise/shadow.go` Shadow=Dedup+Clusterer 前置，Process→Verdict（WouldSuppress/ClusterKey/WouldConverge+Reason 三分类）；cmd/noise.go 汇合接线——CausalSubgraph BFS 域函数（low 门禁天然生效）+ instance→Key 反查 + 空 fingerprint 规范化哈希兜底；env OPS_NOISE_SHADOW=off 关闭 / OPS_NOISE_WINDOW 配窗（非法值启动失败）；IngestCollect 锁外转交防自死锁。16 项测试** | 1 天 | 可对比"应收敛 vs 实际"的数据落库 |
| 1.5 | 簇幂等落库 + 重建 ✅ 2026-09-10 | Redis 簇状态 ↔ alert_event 表双写；Redis 清空后可从表重建。**完成记录（commit b1a613b）：ClusterRecord 落库行（对齐 alert_cluster 列）+ Clusterer.Restore 替换式重建（key 原样保留幂等链不断；空/重复 key 拒绝）；RedisClusterSink（go-redis，HSET 覆盖写=upsert）+ NoiseEngine 批后全量幂等快照双写 + main 启动 Load→Restore 接线；TimescaleDB 真相源 upsert 留 W5 随 pgx（本机无 Docker/DB，SQL 形态已注释留痕）。演练用 miniredis 真实 redis 协议通过：清内存→从镜像重建→续接同一 cluster_key** | 1 天 | 演练：清 Redis 重建成功 ✅（miniredis 演练；真实 DB 侧 W5 合流） |

**里程碑 M4 ✅ 2026-09-10**：影子模式端到端跑通——真实 demo 告警 → 指纹 → 聚类 → 影子标注 → 幂等落库/重建。W4 五个子任务（1.1~1.5）全部完成。

### 阶段 2：W5 API 与最小控制台 —— 预计 5~6 人日

| # | 任务 | 说明 | 工作量 | 里程碑/验收 |
|---|---|---|---|---|
| 2.1 | gRPC 服务接线（SemanticModel）✅ 2026-09-10 | GetTopology（as_of）/GetRecentChanges；映射函数已就绪，**锁内完成映射**（R2 契约）；GetRecentChanges 前先扩 pb（C5 三步走）。**完成记录（commit ae14744）：C5 三步走闭环（proto 补 7 证据字段+映射+枚举字符串化）；GetTopology as_of/邻域裁剪（新原语 Graph.Neighborhood）锁内映射（TopologyAsOf）；GetRecentChanges 窗口查询 start 必填；DialInProcess 真实回环契约测试 7 项** | 1.5 天 | transport 测试 + 契约测试 ✅ |
| 2.2 | REST 网关（簇列表/详情/拓扑查询） ✅ 2026-09-10 | api-gateway 路由挂到现有 mux。**完成记录（commit 5e9e6f2）：4 端点（clusters 列表+state 过滤 / clusters/{key} 详情 / topology as_of+邻域 / changes 窗口），复用 SemanticModelServer 一套语义（gRPC status→HTTP 错误映射），API 输出 ClusterRecord 契约（snake_case），噪声关闭 503 降级；测试 5 项** | 1.5 天 | 接口契约测试 ✅ |
| 2.3 | 控制台最小视图 ✅ 2026-09-10 | 簇列表 + 拓扑图 + 置信度渲染；千节点聚合渲染策略（P2-1）。**完成记录（commit bbffe08）：go:embed 单文件 HTML 零构建链；簇列表（state 过滤/pill 渲染/详情展开/30s 自动刷新）+ 拓扑 SVG 环形布局（置信度三色渲染，ADR-007）+ 节点点击看邻居与 24h 变更 + P2-1 聚合渲染（>300 节点按 type 分桶，钻取留 W6）；GET /console + / 302；本机冒烟通过** | 1.5 天 | 页面可用 ✅ |
| 2.4 | sessionstore 接真实 Redis（O10 收尾） ✅ 2026-09-10 | 会话不蒸发；miniredis/testcontainers 集成测试。**完成记录（commit db4c688）：miniredis 真实协议锁定读写语义——TTL 刷新（活跃 3h 仍可读）/过期/销毁幂等/键前缀隔离 + 构造期守卫回归；main 接线决策：M1 无会话消费方，不做无人调用的接线（README 已同步）；O10 关闭** | 0.5 天 | O10 关闭 ✅ |

**里程碑 M5 ✅ 2026-09-10**：浏览器可看到真实环境的告警簇列表 + 拓扑图（含置信度着色）——/console 页面本机冒烟可用，数据链路 假Prometheus→采集→聚类→REST 全通。W5 四个子任务（2.1~2.4）全部完成。

**里程碑 M5**：浏览器能看到真实环境的告警簇列表 + 拓扑图（含置信度着色）。

### 阶段 3：W6 影子评估与调优 —— 预计 2 人日 + 7 天日历时间

| # | 任务 | 说明 | 工作量 | 里程碑/验收 |
|---|---|---|---|---|
| 3.1 | 接入 ≥1 真实环境跑满 1 周 | 公开 demo + 故障注入可替代 | 挂机 | 数据齐 |
| 3.2 | 准确率评估报告 | 对比应收敛 vs 实际 | 1 天 | **准确率 >85%**（M1 核心验收） |
| 3.3 | 规则包版本化 + 回归测试集 | 可灰度可回滚 | 1 天 | CI 可跑 |

**里程碑 M6**：评估报告签字；不达标则在此迭代（预留 1 周缓冲）。

### 阶段 4：W7 私有化配套 —— 预计 4 人日（可与 W6 并行）

| # | 任务 | 说明 | 工作量 | 里程碑/验收 |
|---|---|---|---|---|
| 4.1 | 单机版打包 | compose + 安装脚本 + 4C8G 资源预检（O6 关闭） | 1.5 天 | 全新机器一键起 |
| 4.2 | 升级生命周期 | 版本化迁移 + 跨版本拒启 + 升级前自动备份（C18） | 2 天 | 跨版本拒启生效 |
| 4.3 | STS 三云预研 | 15 分钟自助配置判定（C22） | 0.5 天 | 结论写入 ADR |

### 阶段 5：W8 出口验收 —— 预计 3 人日

| # | 任务 | 说明 | 工作量 | 里程碑/验收 |
|---|---|---|---|---|
| 5.1 | 压测 | 告警吞吐/降噪延迟/并发 | 1 天 | P95 <30s，容量模型回写 |
| 5.2 | DB owner + Patroni 拓扑（O7） | 责任人指定 | 0.5 天 | O7 关闭 |
| 5.3 | 备份恢复 + 故障切换演练 | RPO<5min / RTO<1h | 1 天 | 演练记录 |
| 5.4 | ADR 补齐 + M1 出口评审 | 每条新决策有 ADR | 0.5 天 | 六项出口标准逐条签字 |

---

## 三、依赖关系

```
1.1 接线清单 ──→ 1.2 指纹 ──→ 1.3 聚类 ──→ 1.4 影子 ──→ 1.5 落库重建
                    │                                    │
                    └── alert_event 表(hypertable 已就绪)  └──→ 3.1 跑影子数据(7天日历) ──→ 3.2 评估
1.5 ──→ 2.2 REST 网关 ──→ 2.3 控制台
2.1 gRPC 接线（依赖 1.1，不依赖降噪）──→ 2.3
4.x W7 私有化（独立，仅依赖 1.1 的可运行部署物）
5.x W8 验收（依赖 M4/M5/M6 全部达成）
```

关键路径：**1.1 → 1.2 → 1.3 → 1.4 → 3.1（7 天挂机）→ 3.2**。影子数据越早开始挂机越好——这是"一天编码省三天日历"的唯一杠杆。

---

## 四、风险与应对

| 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|
| 降噪准确率不达 85% | 中 | M1 延期 | 影子数据 W4 就开跑（提前到挂机）；W6 预留 1 周调优缓冲；仍不达标则缩小场景（单云单集群先过线） |
| 拓扑覆盖率不足 70% | 中高 | 出口标准 2 | demo 环境已 11/11 全 high；真实环境不达标则走"人工确认补录"路径，数据飞轮逐步提升 |
| **compose 部署链路修复后未实测**（X1/X2 昨晚刚修） | 低 | 部署验收翻车 | 列入 1.1 第一件事：本机 `docker compose up` 全链路一次 |
| 一人开发、排期紧 | 高 | 整体延期 | 严格按优先级砍：巡检/RCA/IM/多租户一律 M2；W6/W7 可并行分摊日历时间 |
| 迁移 000002/000003 刚入库未在真实 DB 演练 | 中 | 升级流程返工 | 1.1 实测 compose 时顺带 `make migrate` up/down 各一遍 |
| 预留 API 与接线实现偏差（C5/AsOf 等） | 低 | 接线返工 | 接线时按 R10 checklist 逐项对账（复审报告已固化） |

---

## 五、建议执行顺序（本周即可动）

1. **周一上午**：1.1 接线清单——`docker compose up` 实测 + Host 注册进 main + credential 清扫挂载（0.5 天）；
2. **周一下午 ~ 周三**：1.2 指纹 + 1.3 聚类（2.5~3 天）；
3. **周四**：1.4 影子标注 + 1.5 幂等落库（2 天内完成则影子数据本周开始挂机）；
4. **影子挂机期间并行**：2.1 gRPC 接线 → 2.2 REST → 2.4 sessionstore（2.3 控制台最后）；
5. **下周同时间**：3.1 影子数据满 1 周 → 3.2 评估；
6. W7 私有化穿插在等数据的空档。

**总工作量估算**：约 17~19 人日编码 + 7 天影子挂机日历时间。按每天 3~4 小时有效投入，编码部分约 6~7 周墙钟时间——与 M1 剩余 5 周排期相比偏紧，**压缩手段：2.3 控制台只做最小可用（不做千节点优化，P2-1 挪到 M2 首项）**，可省 1.5 人日。

---

## 六、跟进机制建议

- 每个里程碑（M4/M5/M6）达成时在 `M1执行计划.md` 对应周表打勾并记进度（沿用 W2/W3 的记录风格）；
- 影子评估报告单独成文（W6 交付物），评审后再动 3.3 规则版本化；
- 新决策继续 ADR 先行（现有 ADR-001~007 覆盖面已够 W4 使用，无需新增）。

---

## 执行排期 v2（2026-09-10，W4/W5 完成后重新梳理）

当前基线：commit a04590a。W1~W5 全部完成（M1~M5 里程碑达成），五轮审查 43 项闭环。

### 任务清单与执行顺序

| 序 | 任务 | 目标 | 优先级 | 工作量 | 依赖 |
|---|---|---|---|---|---|
| 1 | **W6-0 评估环境接入** ✅ 2026-09-10 | OPS_PROM_URL 指向 demo/真实 Prometheus + 故障注入脚本；影子数据立即开始积累（日历时间关键路径）。**完成记录（commit af63154）：tools/faultinjector 剧本引擎（A 单节点/B 交换机域聚合/C 静默/D 噪声，/answerbook 已知答案时间线，-scale/-warmup）+ OPS_TOPOLOGY_EDGES 静态边挂起落图机制 + 端到端冒烟命中答案（场景 A：2 alerts new1 merged1）；R1 拍板=故障注入；R2 Docker Desktop 已装（client 29.7.2，daemon 启动待续）；注入器 4 测试 + cmd 集成测试 4 项** | 0.5 人日 | ✅ 环境就绪 |
| 2 | W6-1 评估数据链补全 ✅ 2026-09-10 | 逐告警 Verdict 落库 + alert_cluster 真相源 upsert。**完成记录（commit 27c24d4）：VerdictRecord 对齐 alert_event（评估字段进 payload JSONB）；PGClusterSink（pgx v5，ON CONFLICT upsert + evidence JSONB）+ RedisVerdictSink（RPUSH+LTRIM 20 万上限）+ multiRecordSink 双写；OPS_DB_DSN 接线（不可达降级 redis-only）；TimescaleDB 真实打库测试通过（本机 compose 栈自动恢复，DSN 门控集成测试）** | P0 | 1.5 人日 → 实际 ~0.5 人日 | ✅ |
| 3 | W6-2 挂机 7 天 **进行中** | 影子数据积累满 1 周。**2026-09-10 启动：demo 全链路（注入器+opscopilot+Redis+TimescaleDB）已由蔡总终端跑通，alert_event 开始积累。注意：电脑勿睡眠（电源设置）、重启后重跑 run_demo.sh（注入器重启会重置答案簿时间线，评估窗口从重启时刻分段）。** | P0 | 日历时间 | 1+2 完成 |
| 4 | W6-3 准确率评估 **脚本已就绪** | tools/evaluate.py（commit fdf0ae0）：verdict 按 answerbook 场景段打分，退出码=是否达标。**评估须真实时序（scale=1.0+10m 窗口），--fast 缩放假象已注明**。挂机满 7 天后执行即出报告 | P0 | 1 人日 → 剩 ~0.2 | 3 |
| 5 | W6-4 规则包版本化 | 规则可灰度可回滚 + 回归测试集进 CI | P1 | 1 人日 | 4 达标 |
| 6 | W7-1 单机版打包 | compose 优化 + 安装脚本 + 4C8G 资源预检（O6） | P1 | 1.5 人日 | 无（并行） |
| 7 | W7-2 升级生命周期 | 版本化迁移 + 跨版本拒启 + 升级前自动备份（C18） | P1 | 2 人日 | 无（并行） |
| 8 | W7-3 STS 三云预研 | 15 分钟自助配置判定（C22），结论写入 ADR | P2 | 0.5 人日 | 无 |
| 9 | W8-1 压测 | 告警吞吐/降噪延迟/并发，P95<30s，容量模型回写 | P2 | 1 人日 | 4 |
| 10 | W8-2 DB owner + Patroni | 责任人指定 + 拓扑确认（O7） | P2 | 0.5 人日 | 7 |
| 11 | W8-3 备份恢复演练 | RPO<5min / RTO<1h 演练记录 | P2 | 1 人日 | 7 |
| 12 | W8-4 M1 出口验收走查 | 出口标准逐项核对 + 交付文档 | P3 | 0.5 人日 | 4+9+10+11 |

技术债（穿插，不占关键路径）：① resolved 簇内存历史上限；② CI race job 覆盖确认；③ Redis sink pipeline / union-find（按量级触发）。

### 风险与待确认事项

| # | 风险/事项 | 影响 | 需要的动作 |
|---|---|---|---|
| R1 | **影子评估的 ground truth 未定义**：demo 告警没有"应收敛"的真值标注 | 3.2 报告无法客观打分，M1 出口标准悬空 | **需拍板评估方法**：故障注入已知答案（推荐，可复现）或人工标注抽样 |
| R2 | Docker Desktop 本机不可用 | W7-1 打包验证、pgx testcontainers 集成测试受阻 | 确认替代环境（CI runner / 其他机器）或本机安装 Docker |
| R3 | 真实环境接入授权 | 接客户/公司 Prometheus 需网络与安全审查 | 提前申请；demo 环境可先行启动挂机 |
| R4 | 准确率不达 85% | M1 延期 | 已预留 1 周调优缓冲；仍不达标则缩小场景（单云单集群先过线） |
| R5 | pgx 新依赖引入 | 增加镜像体积与 CVE 面 | 按 ADR 惯例补一条决策记录（可与 W6-1 一并） |

关键路径：1 → 3 → 4 → 9/12；W7 三项全程并行。预计净人力 ~11 人日 + 7~14 天日历时间。
