# OpsCopilot M2 出口评审纪要 — 2026-09-13

- **评审对象**：M2（W9~W11，"影子转正 + 通知闭环 → 事件域主工作面 → RCA 可用 + 交付收尾"全链）
- **基线范围**：`6f433b9..059cd26`（约 50 提交）+ 出口收口轮（本文档随附的 P1 三修 + P2 文档收口）
- **输入材料**：`docs/reviews/全局代码审核报告-第十轮-2026-09-13.md`（放行结论 P0=0/P1=3/P2=17）、`docs/M2执行排期-W9到W11.md`、`docs/容量基线-2026-09-12.md`、`docs/reviews/rca-eval-2026-09-12-2332/report.md`、`docs/W9出口演练记录-enforce端到端.md`、`docs/reviews/round10-P2遗留-M3清单.md`
- **性质声明**：本纪要为**代笔稿**——记录出口对账证据与放行建议，**最终签字留人**（见 §四）。

## 一、出口标准对账

M2 排期原文定义（`docs/M2执行排期-W9到W11.md`）：

> **W11 出口（= M2 出口）**：全量测试绿 + 第十轮全局审核 + M2 出口评审纪要。

叠加排期各周出口行与 M2 目标句（"告警进来 → 自动降噪 → 建单/通知 → 辅助定位 → 人工闭环"完整价值链）与里程碑（M2-M1/M2-M2/M2-M3），六项验收逐条对账：

| # | 验收项（排期原文口径） | 状态 | 证据（commit / 报告指针） |
|---|---|---|---|
| 1 | **W9 出口**：enforce 模式端到端演练通过（注入器 4 场景 → 降噪拦截 → 建单 → 通知送达） | ✅ 达成 | `06c472e`；拦截 15/21 + 建单 7 + 双渠道通知 6+6 + 评估 9/9 段 100% + 延迟 P95 0.48s——`docs/W9出口演练记录-enforce端到端.md` |
| 2 | **W10 出口**：控制台事件页"看板 + 工单"双职能，时间线/SLA/KPI 全部真实数据 | ✅ 达成 | W10-1 `ecedc47`+`db4dd57`；W10-2/3 `ab3b1d0`+`25bc2c3`（契约 17/18/19 + `TestKPIEndpointMatchesManualReckoning`）；W10-6 `3d7f44c`；W10-4/5 决策与盘点 `89a18c2`..`555d705`——排期 W10 表行 + 落地注记 |
| 3 | **M2-M3 / W11 出口**：RCA 可用 + W7 前置清零（upgrade + 备份恢复演练） | ✅ 达成 | RCA 六步 `16d4b91`+`d86bb6e`（评测 4/4，见 §二）；web 三块 `6c6febc`；Runbook `3b6334d`+`98dd6c9`+`059cd26`；upgrade+闸门 `ca25a59`+`d7b3a71`，真库演练 `cmd/opscopilot/upgrade_drill_pg_test.go`（本轮再补 golang-migrate 形状用例）；容量基线含备份对账（RTO/RPO 分钟级成立，`docs/容量基线-2026-09-12.md` §7） |
| 4 | **全量测试绿**（含真库回归，非静默 skip） | ✅ 达成 | 出口收口轮本机复跑：gofmt clean / vet 无告警 / build OK / `go test ./... -count=1`（`OPS_TEST_PG_DSN` 真库）**22 包 ok、0 FAIL**；Leader/AutoAttach/RcaAuto 与双形状升级演练真跑全绿（race 门禁由 CI 兜底，见 #5 与 §三 R-1） |
| 5 | **第十轮全局审核** | ✅ 完成 | `docs/reviews/全局代码审核报告-第十轮-2026-09-13.md`：**P0=0、P1=3、P2=17，阻断项清单为空**，结论"放行 M2 出口"；三个 P1 已在出口收口轮修复（见 §三 R-1 各行"收口落点"） |
| 6 | **M2 出口评审纪要** | ✅ 本文档 | `docs/M2出口评审纪要-2026-09-13.md`（签字留人） |
| — | **M2 目标价值链**（背景对账：告警进来→降噪→建单/通知→辅助定位→人工闭环） | ✅ 闭合 | ingest 认领租约（`000016`/`ingest_queue.go:170-224`）→ enforce 判决（`b27fbc8`，ADR-011）→ 自动挂簇建单 `3d7f44c` + 通知升级（`59ac7ec`/`909d84b`）→ RCA 4/4（§二）→ 人工面：时间线/SLA/KPI/Runbook 记录版（`3b6334d`）闭环 |

## 二、容量与安全读数

**容量**（`docs/容量基线-2026-09-12.md`）：

- 入队链路：单实例 ≥4000 events/s 起步、4000/s 档 P99 62→225ms 为实测拐点；双实例 TPS ≈2× 近线性（7,964），消费认领排空 2.1× 线性（P95 时延受共享 PG 争抢不线性——已列入部署注意项）。
- 建单吞吐：默认 ≈4 ev/s、调优 ≈160/s——ADR-016 灰度方案的硬依据（默认配置不开 autoCreate）。
- 判决延迟：fired→verdict P95 随单轮活跃告警数线性（200→0.9s，1000→2.5s，2000→4.5s），**均 <30s 红线**；30 分钟浸泡 RSS/sink 队列/drops 全程平稳（P99 8.5s 零星慢轮为观察项，非回归）。
- 备份/还原分钟级（L 档 30 天 ~20GB 线性外推），RPO<5min/RTO<1h 成立。

**效果**：RCA 评测集（`docs/reviews/rca-eval-2026-09-12-2332/report.md`，生产挂簇路径证据版）**top-1 命中 4/4 = 100%**（门禁线 85%）、top-3 4/4、证据完整率 4/4、guards 3/3，四单元 note 全走生产自动路径零回退桥接；影子降噪准确率评估 36/36 段 100%（`docs/W6-3影子降噪准确率评估报告-正式-2026-09-11.md`）。

**安全**（round10 报告 §域 C，四条线全过）：**P0 = 0**；llmgw/密钥 canary 未被绕过（key 恒走 Authorization 头、错误值脱敏、三口封死出口）；REST 写端点零未鉴权（401 先于 415/503）、CORS 默认 fail-fast、console.html 冻结未破；SQL 注入面零发现（全参数化、DDL 走 `pgx.Identifier.Sanitize`）；数据正确性三件套（acked_at/SLA/KPI × 租约扇出）逐边界核查无致命面。

## 三、遗留风险分级

| 级别 | 项 | 状态 | 读数/条件 |
|---|---|---|---|
| P1-1 | `runLeaderGated` 唤醒竞态（ADR-012 单 owner 违例窗口） | **已修** | `leader_gated.go` 捕获-校验对调（先 `Notify()` 后判 `IsLeader()`，与 `leader.go:143` 契约姿势对齐）+ 竞态注释 + 回归用例 `TestRunLeaderGatedCaptureThenDemoteNoWakeupLoss`（channel 语义直断，不造时序测试） |
| P1-2 | `upgrade` 与 golang-migrate 主线表形状相撞（42P10 半程退出）+ 未跟踪存量库误拒启（P2-D4） | **已修** | `version.go` 形状探测分流：PK 形状 ON CONFLICT；golang-migrate 形状按"单行=当前版本"约定走**同事务 DELETE+INSERT (version, dirty=false)**（原子性理由入注释）+ 兜底 WHERE NOT EXISTS；闸门新增"读 0 + 域表存在 → 放行+WARNING 提示登记"；真库双形状用例（`TestUpgradeDrillOnGolangMigrateShape` 等）本机真跑全绿；README 拒启矩阵补格 |
| P1-3 | CI 真库回归接线空白 | **✅ 已确认（2026-09-15，4A 首跑）** | `ci.yml` 增 timescaledb service（`timescale/timescaledb:2.29.2-pg17` 与 compose 同源，`pg_isready` health check）+ `OPS_TEST_PG_DSN`（仅测试 DSN，不设 `OPS_DB_DSN`）+ `bash scripts/reset_test_pg.sh --wipe` 从零建库（脚本原生支持 env 传 DSN，零改动）；Test/Test-race 补 `-count=1`。**首跑确认**：run #86 曾因 web-walkthrough 的 preview IPv6-only 监听红灯（vite 默认绑 `[::1]`，CI 用 `curl 127.0.0.1` IPv4 轮询必超时），`fix(ci)` `8e8932e`（`--host 127.0.0.1` + 走查 URL 同步）后 **run #87 四 job 全绿**：build-and-check（gofmt/vet/build/真库 Test/`-race`/边界）、goplugin-smoke ×2、web-walkthrough（5 视口 × 6 路由）。真库实跑依据：`OPS_TEST_PG_DSN` 注入 + `reset_test_pg.sh --wipe` 建库步骤 success + Test 步骤 success（pgstore 子用例 skip 条件为 DSN 空，DSN 非空必然 RUN） |
| P2 | 审核遗留 15 项（窄窗口/卫生/文档类） | **不阻塞** | 全部转 `docs/reviews/round10-P2遗留-M3清单.md`（一行一项带文件指针）；本轮已顺手清 2 项（D4 随 P1-2、D6 文档数字面：README/配置清单 68→69 实数=69，OPS_*67+REDIS_*2，代码 grep 核对；排期 W10-2/3 回填 `ab3b1d0`+`25bc2c3`、W11 六行标 ✅ 附提交号与一句证据、进度行刷新） |
| 观察项 | 判决延迟 P99 零星慢轮、双实例共享 PG P95 劣化 4.6×、`OPS_INCIDENT_AUTOCREATE` 默认 off（ADR-016 待拍板） | 记录在案 | 前两者已在容量基线标注；autoCreate 翻默认走 ADR-016 三段灰度 + 带参复跑容量基线硬关口 |

## 四、结论

**建议放行 M2 出口**，附带条件：

1. **CI 首跑确认**（出口后 48h 内）：P1-3 workflow 首跑绿灯、真库用例确认实跑（非 skip）；红则按清单回修，不追认既往。
2. **M3 排期入池**：P2 遗留 15 项（`round10-P2遗留-M3清单.md`）随 M3 计划认领；heldConn 锁外 Acquire、eval trap 前移、redactDSN keyword 分支建议优先（均为窄修）。
3. **升级通道宣贯**：主线仍以 `migrate` CLI 为准，`opscopilot upgrade` 现对两种台账形状安全可用；未跟踪存量库新二进制不再误拒启（README 矩阵已更新）。
4. **ADR-016 autoCreate 默认值维持 off**，转正走灰度硬关口，与本出口解耦。

**签字**：本轮收口与评审由 AI 代理执行，本纪要为代笔稿——**出口放行的最终签字留给人**（评审主持：____ · 出口负责人：____ · 日期：____）。
