# OpsCopilot 独立前端 —— Vite 环境变量样例（复制为 .env.local，勿提交）
# 只有 VITE_ 前缀的变量会进前端包——本文件不放真实密钥。

# REST/SSE base URL。留空 = 同源相对路径（推荐口径）：
#   - 开发：vite dev server 把 /api 代理到后端（代理目标用 VITE_API_PROXY 覆盖）
#   - 生产：后端静态目录托管 dist/（同源、零 CORS 变更，见 ADR-013 第 5 条）
# 仅当前端独立部署且后端显式配了 OPS_CORS_ORIGIN 白名单时，才填绝对 URL：
# VITE_OPS_API_BASE=http://127.0.0.1:8080
VITE_OPS_API_BASE=

# 写操作共享密钥（X-OpsCopilot-Token 的值，对应后端 OPS_WEBHOOK_TOKEN）。
# 警告：VITE_ 变量会打进构建产物，仅限内网个人调试；正式部署留空，
# 改在界面 Token 输入框录入（sessionStorage 关页即清）或由反向代理注入。
VITE_OPS_TOKEN=

# 仅开发期生效：vite 代理目标（后端 OPS_LISTEN_ADDR 默认 127.0.0.1:8080）。
# VITE_API_PROXY=http://127.0.0.1:8081
