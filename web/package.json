{
  "name": "opscopilot-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "OpsCopilot 独立前端（优化方案 #9 前端分离）：Vite + React + TS，只消费后端 REST/SSE",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
