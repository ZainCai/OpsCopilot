# OpsCopilot 功能点清单（原型对照缺口 · M2 候选 Backlog）

> 生成日期：2026-09-10 · 基线：commit 60548aa
> 参照系：`console_source_sample`（EagleOps 原型 9 个页面）× 当前已实现能力（W1~W6-1）。
> 命名风格对齐现有模块名（connector/topology/noise/console/…）与 W 阶段任务惯例。
> 每个功能点：名称 / 简要说明 / 所属模块 / 原型来源 / 优先级建议。

## 一、假设与范围说明

1. **"缺失部分"的界定**：以 EagleOps 原型的页面结构为需求参照，与当前已实现能力对照后的差距功能点；纯演示性内容（模拟数据层 `data.js`、源码浏览页 `source.jsx`、调试面板 `tweaks-panel.jsx`、妙搭平台运行时依赖）不在范围内。
2. 当前已实现的"簇（cluster）"与原型"事件（incident）"是两个层级：簇是降噪产物（机器视角），事件是运维工单（人的视角）——原型以事件为中心，这是最大的结构缺口。
3. 影子降噪转正（实际拦截通知）是 M1 出口后的第一个动作，原型虽无独立页面但属隐含需求，已列入。

## 二、现有功能结构（基线，不重复列入清单）

| 模块 | 已有能力 |
|---|---|
| connector | Prometheus / Azure 只读连接器、Host 调度、凭证只读闸门、静态拓扑边 |
| topology | 时态拓扑（bi-temporal）、as_of 时点查询、邻域裁剪、置信度体系（ADR-007）、变更库（幂等/证据字段） |
| noise | 指纹去重、时间窗聚类、故障域聚合、影子判决、簇幂等落库（Redis 镜像 + TimescaleDB 真相源）、恢复重建 |
| transport/api | gRPC SemanticModel（GetTopology/GetRecentChanges）、REST 只读网关（clusters/topology/changes）、CORS |
| console | 簇列表/详情、状态过滤、统计卡、拓扑环形图（置信度三色/千节点聚合）、节点 24h 变更、自动刷新、EagleOps 皮肤 |
| evaluation | 故障注入器（4 场景剧本 + answerbook）、evaluate.py 准确率评估 |
| 工程 | 双 Redis 实例纪律、会话存储、3 次迁移（TimescaleDB）、边界检查 CI、Docker compose |

## 三、缺失功能点清单（11 项）

### 模块：incident（新增模块——事件域，原型最大缺口）

| # | 名称 | 说明 | 原型来源 | 优先级 |
|---|---|---|---|---|
| F-01 | 事件实体与生命周期 | 新增 incident 实体（incident_id / 标题 / 严重级 / 状态机 open→acked→mitigated→resolved / SLA 时钟），DB 迁移 + API；簇是降噪产物，事件是人的工单，两层解耦 | incidents.jsx / incident.jsx | P0 |
| F-02 | 簇→事件关联 | 告警簇升级/合并进事件（一事件多簇），确立"告警→簇→事件"动线；原型行点击直达事件详情的体验以此为前提 | alerts.jsx（行点击 → incident） | P0 |
| F-03 | 事件时间线 | 混合时间线：告警进出 / 变更记录 / 人工处置动作按时间排列（原型 tl-item 形态），数据源复用 alert_event + change_record | incident.jsx | P1 |
| F-04 | 事件 SLA 时钟 | 事件级 SLA 进度条（目标时长 + 超时标记，原型 sla-bar 形态）；M1 只记录不升级 | incident.jsx | P2 |

### 模块：rca（新增模块——根因分析）

| # | 名称 | 说明 | 原型来源 | 优先级 |
|---|---|---|---|---|
| F-05 | RCA 流水线 | 六步证据流水线（原型 pipeline 形态：取证→假设→验证→…），输入=故障时刻 T0 的 as_of 拓扑 + 窗口内变更 + 簇证据；已有 API（GetTopology as_of / GetRecentChanges）正好是它的数据源 | rca.jsx | P1 |
| F-06 | 根因标注与置信度 | RCA 结论挂载根因节点（--rootc 标注色）+ 置信度条（原型 conf-bar 形态）；与 ADR-007 置信度体系同源 | rca.jsx / incident.jsx | P1 |

### 模块：console（控制台增强）

| # | 名称 | 说明 | 原型来源 | 优先级 |
|---|---|---|---|---|
| F-07 | 运维 KPI（MTTA/MTTR/闭环均值） | 仪表盘 KPI 行：确认时长/恢复时长/平均闭环（原型 dashboard"平均闭环 42 分钟"口径），数据来自事件时间线（依赖 F-01） | dashboard.jsx | P1 |
| F-08 | 事件详情页（控制台） | 控制台新增事件路由：详情面板（根因/证据链/时间线/SLA），复用 EagleOps 皮肤组件（chain 证据链时间线形态） | incident.jsx | P1 |
| F-09 | 拓扑画布升级 | 环形圆点图 → 原型 topo-canvas 形态：矩形节点卡（名称+子标）、悬浮 tip、边 hover 高亮、根因链路高亮 | topo-canvas.jsx | P2 |
| F-10 | 告警中心统计视图 | 簇列表 list/stat 双视图（按节点/严重级分桶 + 占比），聚合渲染阈值内也可切统计 | alerts.jsx StatView | P3 |

### 模块：notify（新增模块——通知与处置，影子转正的前提）

| # | 名称 | 说明 | 原型来源 | 优先级 |
|---|---|---|---|---|
| F-11 | 通知抑制执行 + 渠道 | W6 评估达标后降噪"转正"：WouldSuppress 真正拦截通知；通知渠道配置（IM/webhook）、值班升级规则；原型 settings 的通知渠道/值班口径 | settings.jsx / app.jsx 通知铃 | P0（转正后即 P0） |
| F-12 | 处置手册（Runbook） | 事件挂载处置手册与执行记录（原型 remediation 页：自动修复建议 + 执行状态 + 置信度）；M1 仅记录与展示，不自动执行 | remediation.jsx | P2 |

### 模块：ai（新增模块——AI 助手，ADR-003 单出口约束下）

| # | 名称 | 说明 | 原型来源 | 优先级 |
|---|---|---|---|---|
| F-13 | AI 助手对话抽屉 | 全局对话窗（原型 ai-chat 抽屉形态）：追问事件/RCA 结论、解释证据链；LLM 调用必须走 ADR-003 单出口网关；M1 可只做只读问答（不执行操作） | ai-chat.jsx | P2 |

## 四、依赖与排期建议

```
F-01 事件实体 ──→ F-02 关联 ──→ F-03 时间线 ──→ F-07 KPI ──→ F-08 事件详情页
                    └────────→ F-04 SLA
F-05 RCA 流水线 ──→ F-06 根因标注（数据源已有，W6 结束即可开工）
F-11 通知抑制（W6 评估达标后立即，M1 出口动作）
F-09/F-10 控制台增强、F-12 Runbook、F-13 AI 抽屉 —— 并行插入不占关键路径
```

- **P0 三件**：F-01（事件实体）、F-02（关联）、F-11（通知转正）——构成"M1 验收后第一个可交付闭环"（降噪真的开始省事了）。
- **P1 四件**：RCA 双件 + 控制台双件——原型的核心差异化体验。
- 建议以 **W9（事件域）/ W10（RCA + 转正）/ W11（体验增强）** 三个阶段纳入计划，每阶段含对应 DB 迁移与 API 契约扩展。
