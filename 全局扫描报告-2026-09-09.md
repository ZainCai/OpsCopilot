# OpsCopilot 全局扫描报告（2026-09-09 20:40）

**扫描范围**：全仓 44 个 Go 文件（主模块约 7200 行 + tools/smoke 子模块）、Dockerfile / docker-compose / Makefile / CI workflow / gen.sh / migrations SQL / .gitignore / README / ADR 文档。
**扫描方法**：工具门禁全量执行（gofmt / go vet / go build / go test / 模块边界脚本）+ 14 个修复提交（f55383d→d431b81）逐 diff 自审 + 周边文件通读 + 安全面 grep（密钥/日志/TODO/依赖）+ 文档与实态交叉核对。
**基线状态**：此前全局复审 18 项问题（S1+G1~G5+C1~C12）已全部修复闭环；本次为修复后全量复扫。

---

## 一、总体结论

代码主体健康：门禁全绿（gofmt 零输出、vet 零告警、11 包测试全过、模块边界通过、-race 由 CI 兜底）、无 TODO/FIXME 残留、无硬编码密钥入库（.env 已被正确 ignore）、集成测试用 build tag 隔离。

**本轮发现集中在"代码修复与部署/文档层的衔接缝"**：两处严重问题都在部署路径（而非业务代码）——Dockerfile 的 Go 版本落后于 go.mod，以及 S1 回环绑定修复后 docker-compose 的端口映射被"架空"。另有 CI 生成脚本在 Linux 下的产物位置错误与 README 运维文档缺口。

---

## 二、问题清单

### 严重（部署路径当前不可用，建议立即处理）

| # | 位置 | 问题 | 修复建议 |
|---|---|---|---|
| X1 | `Dockerfile:1` | **镜像构建必败**：builder 基础镜像 `golang:1.22`，而 go.mod 声明 `go 1.23`——`go mod download` 一步即报 "go.mod requires go >= 1.23"。`docker build` / `docker compose up --build` 全部不可用。 | builder 镜像升级为 `golang:1.23`（或更高）；顺带核对 CI 的 setup-go 1.23 已一致。 |
| X2 | `docker-compose.yaml:66-69` | **S1 修复的部署回归**：应用容器内默认监听 `127.0.0.1:8080`（S1 安全修复），但 compose 仍声明 `ports: "8080:8080"`——宿主端口映射永远打不通，容器化部署功能失效。 | compose 的 app 服务补环境变量 `OPS_LISTEN_ADDR=0.0.0.0:8080`（容器隔离本身就是安全边界，容器内绑全接口是标准做法）；同时补 `OPS_WEBHOOK_TOKEN` 占位（引用 `${OPS_WEBHOOK_TOKEN:-}`），消除每次启动的未鉴权警告并让 compose 部署默认带准入。 |

### 一般（功能/工具缺陷，建议尽快处理）

| # | 位置 | 问题 | 修复建议 |
|---|---|---|---|
| X3 | `scripts/gen.sh` 非 Windows 分支 | **生成产物位置错误**：`--go_out=$OUT_DIR` + `paths=source_relative` 会把 `topology.pb.go` 生成到 `internal/contracts/` 根目录（package pb 出现在错误目录），与仓库实际布局 `internal/contracts/pb/` 不一致；Windows CJK 兜底分支反而是正确的（生成到临时目录再拷入 `pb/`）。CI 不跑 gen.sh 所以未暴露，本地 Linux/macOS 重新生成契约时必踩。 | 非 Windows 分支 `--go_out` 改为 `$PKG_DIR`（或统一走临时目录再拷贝，与兜底分支合流）。 |
| X4 | `README.md` | **运维对接文档缺口**：S1/G5 改变了行为但文档未跟上——`OPS_LISTEN_ADDR`/`OPS_WEBHOOK_TOKEN` 两个安全开关无说明；变更 webhook 的对接方式（POST /api/v1/changes、请求体格式、401 鉴权、重复 ID 幂等返回 200+duplicate）无任何记录；Git/Jenkins 接入方无从下手。 | README 增补"运维接入"一节：环境变量表、webhook 请求/响应示例（含 401 与幂等语义）、compose 部署注意事项（X1/X2 修复后一并写）。 |

### 建议（维护性/健壮性，积累后批量处理）

| # | 位置 | 问题 | 修复建议 |
|---|---|---|---|
| X5 | azure/prometheus vs pkg/httpx | 响应体上限默认值 **32MiB 三处重复定义**（两连接器各自 `defaultMaxResponseBytes` 本地常量 + `httpx.DefaultMaxResponseBytes`）；C1 抽共享层后这里没跟上，将来调上限会漏改。 | 两连接器改引用 `httpx.DefaultMaxResponseBytes`，删除本地常量。 |
| X6 | `docker-compose.yaml` | redis-alert / redis-cache 无 healthcheck（depends_on 仅 service_started，app 可能在 Redis 未就绪时启动——go-redis 懒连接下暂不致命，但迁移/首写会撞）；app 自身有 /healthz 却未配置容器 healthcheck。 | 两个 Redis 加 `redis-cli ping` healthcheck；app 加 curl /healthz healthcheck（distroless 无 shell，可用谷哥 mediated 或改 IMAGE）。 |
| X7 | `docker-compose.yaml` / `.env.example` | DB 默认口令 `opscopilot` + `sslmode=disable`——开发环境可接受，但模板未注明生产约束。 | `.env.example` 加注释：生产必须覆盖口令并启用 TLS（ADR-006 生产用 Patroni + 内网）。 |
| X8 | 预留 API | `connector.WithConnTimeout`、`credential.Len/Stats/SweepExpired` 当前仅测试调用（生产接线未开始）——注释已声明意图，属正常预留。 | 提醒：W4 接线时记得挂载——尤其 credential 的周期清扫调度（C9 注释已固化该约定）；勿让预留长期无消费者。 |
| X9 | `Dockerfile` | 无 HEALTHCHECK；`EXPOSE 8080` 未说明与 OPS_LISTEN_ADDR 的配合关系（X2 修复时一并补注释）。 | 运行阶段镜像加 healthcheck 或在 compose 层做（见 X6）。 |

---

## 三、健康确认（本轮验证通过的方面）

- **工具门禁**：gofmt 全仓零输出（含 tools/smoke 子模块，CI 同口径）、`go vet` 零告警、`go build` 通过、`go test ./...` 11 包全过、模块边界脚本通过。
- **安全面**：webhook 鉴权 constant-time 比较无误、Token 为空时显式降级并打警告（语义一致）；无硬编码密钥（compose 默认口令为开发模板值且与 .env.example 一致，真实 .env 未入库已验证 ignored）；连接器错误信息含响应体摘要但不含凭证（Authorization 仅在请求头）。
- **依赖面**：根模块与 tools/smoke 子模块各自 go.sum 完整、离线可构建；无未使用依赖（xxhash 为 redis 真实消费者）。
- **代码卫生**：全仓零 TODO/FIXME/XXX；集成测试以 `go:build integration` 隔离于默认测试；G5 幂等 / G3 部分成功 / C11 错误上下文等新语义均有回归测试锁定。
- **文档**：ADR-001~007 与实态一致；C5 契约差异、C9 内存边界等约定均已固化在代码注释。

## 四、修复顺序建议

1. **X1 + X2**（部署链路，约 10 分钟）：Dockerfile 升 Go 版本 + compose 补两个环境变量——容器化部署恢复可用。
2. **X3**（gen.sh 产物位置）：下次重新生成契约前修掉，避免生成物污染 contracts 根目录。
3. **X4**（README 运维文档）：与 X1/X2 同一提交补齐最顺手。
4. **X5~X9**：随下次重构批次批量处理。

---

*扫描执行：全局复扫于 14 个修复提交之后（HEAD = d431b81）；本报告与《全局代码复审报告》互补——前者管"修复是否引入新问题"，本报告管"修复后的整体健康度与部署链路"。*

---

## 五、修复状态（2026-09-09 21:00 闭环）

| # | 提交 | 说明 |
|---|---|---|
| X1 | `3c43754` | builder 升 golang:1.23，docker build 恢复可用 |
| X2 | `3c43754` | compose app 补 OPS_LISTEN_ADDR=0.0.0.0:8080 + OPS_WEBHOOK_TOKEN 透传，端口映射恢复可达 |
| X3 | `3c47030` | gen.sh 非 Windows 分支 --go_out 改 $PKG_DIR，与兜底分支统一；bash -n 校验通过 |
| X4 | `3c43754` | README 新增"运维接入"：环境变量表、webhook 示例与状态码语义、compose 注意事项 |
| X5 | `55d6726` | 32MiB 上限统一到 httpx.DefaultMaxResponseBytes，两连接器删本地常量 |
| X6 | `3c43754` | 两个 Redis 补 redis-cli healthcheck；app depends_on 升 service_healthy |
| X7 | `3c43754` | .env.example 补生产约束注释（口令覆盖/TLS/OPS_* 开关） |
| X8 | — | 预留 API 接线提醒已在代码注释固化（W4 接线时执行） |
| X9 | `3c43754` | distroless 无 shell 镜像层 HEALTHCHECK 不可行，已在 Dockerfile/compose 注明走 GET /healthz 由编排层负责 |

**收尾验证**：compose config 校验通过、gen.sh bash -n 通过、gofmt/vet/build/test 11 包全绿。X1~X9 全部闭环。
