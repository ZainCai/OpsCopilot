# OpsCopilot 全局代码审核报告（第五轮 · 2026-09-10）

> 审核范围：commit `db4c688` 全库（12 包，Go 源码约 1.1 万行，测试占比 >45%）。
> 方法：W5 新增代码逐文件精读 + 既有修复回归面交叉检查 + 门禁全跑（build/vet/test/gofmt/边界检查）。
> 前置：三轮复审 36 项 + 第四轮扫描 4 项已闭环。
> **结论：发现 3 项实质问题（1 高 / 2 中）+ 8 项低风险观察；3 项实质问题已当场修复并带回归测试推送。**

---

## 一、按严重程度分级的问题清单

### 高（已修复）

**G1 · 落库签名失败不回滚 → 丢失写入永不补齐**
- 位置：`cmd/opscopilot/noise.go` — `collectDirtyClusters` / `persistClusters`
- 原因：第四轮 F3 引入签名去重时，签名在**锁内**先更新为"已持久化"，然后锁外执行写入。写入失败时签名不回滚——下一批签名比对判定"无变化"，**失败的写入永远不会重试**。原注释"丢失的写入由下一批的签名比对自动补齐"与实际行为相反。对 resolved 簇（状态不再变化）等于**永久丢失**，直接违反 ADR-001"真相在库、Redis 丢失只是重放延迟"的核心承诺。
- 修复：`SaveCluster` 失败即锁内删除该 key 的签名，下一批自动重写；回归测试 `TestPersistFailureRollsBackSignature`（失败→恢复→断言补写发生）。
- 教训：注释宣称的恢复语义必须有用例证明——"自动补齐"这种话没有测试就是谎言。

### 中（已修复）

**G2 · 零值 StartsAt 告警掉进 1970 桶且被去重吞掉**
- 位置：`cmd/opscopilot/noise.go` — `toEvent`；根源在 `internal/connector/prometheus/prometheus.go` `parseTime`（解析失败返回零值）
- 原因：上游 `startsAt` 缺失或格式坏 → 零值 `OccurredAt` 一路透传进 noise——①聚类窗口桶 `零值.Unix()/600 = 0`，所有零时间告警聚进 `c:<fp>@0` 同一桶，时间字段显示 0001 年；②去重器按事件时间判定，零值时间戳使同指纹后续告警永远命中"窗口内"被抑制。**告警静默消失**。
- 修复：装配层兜底——`OccurredAt` 零值改用当前观察时刻（noise 包保持纯语义，时间修补责任在 cmd/）；回归测试 `TestZeroStartsAtFallback`（断言簇时间不是 0001 年）。

**G3 · clusters 列表无上限，响应随历史膨胀**
- 位置：`cmd/opscopilot/rest_gateway.go` — `handleClusters`
- 原因：7 天影子期 resolved 簇持续累积（这是设计内行为），无上限列表在第 7 天会拖垮浏览器与带宽。
- 修复：`limit` 参数（默认 200、上限 1000），`Clusters()` 按 key 有序保证截断确定性，响应带 `truncated` 标记；非法 limit 400。回归测试 `TestRESTClustersLimit`。

### 低（记录在案，暂不改代码）

| # | 位置 | 观察 | 建议 |
|---|---|---|---|
| L1 | `cmd/noise_store_redis.go` | 逐条 HSET，每批 N 次网络往返 | 簇数上百或引入 DB 双写时改 pipeline |
| L2 | `cmd/noise.go` `reachable` | 每对（簇节点×告警节点）一次 BFS | 千节点拓扑前改 union-find 预计算连通分量（O(V+E) 一次） |
| L3 | `internal/transport` | DialInProcess 单连接模型：第二个并发 gRPC Dial 的 Accept 永久阻塞 | 形态约束已在 R6/R7 注释留痕；多客户端需求出现时换 bufconn |
| L4 | `cmd/semantic_model.go` `changeToProto` | 零值 OccurredAt 会格式化为 `0001-01-01T00:00:00Z` | 实际不可达（Record 会填充时间）；若开放外部导入路径需加防御 |
| L5 | `console.html` | 自动刷新的并发 fetch 无竞态保护（慢响应可能覆盖新响应） | M1 单用户低风险；W6 加请求序号丢弃旧响应 |
| L6 | `cmd/main.go` | 启动恢复 `LoadClusters` 未显式设超时（依赖 go-redis 默认 3s 读超时） | 可接受；W7 私有化预检脚本中确认 |
| L7 | REST 全部端点 | 只读无鉴权 | S1 写路径准入不涉及；默认 loopback-only，非回环暴露必须前置鉴权反代（main 启动警告已覆盖） |
| L8 | 全库 | 本机无 gcc，`-race` 无法本地跑 | 依赖 CI race job；确认其覆盖 noise/cmd 新并发路径 |

## 二、各维度评估

| 维度 | 评价 |
|---|---|
| 架构设计 | **优**。internal 禁互 import 纪律经 5 轮审查无一破例；noise 以函数注入保持零依赖；gRPC/REST 一套语义两个门面。模块边界脚本 + ADR 决策链是全库最强资产。 |
| 代码质量 | **良+**。注释与实现同步（本轮抓到一处注释与行为相反的 G1，已修）；命名清晰；无超长函数。扣分点：测试占位代码两次混入初稿（均自查清除），说明"写完即读"的习惯还需固化。 |
| 潜在 bug | **中**。G1/G2 都属于"错误/边界路径没有测试"的老类别——happy path 的测试密度已经很高，异常路径是下一步测试投入方向。 |
| 安全 | **良**。XSS 全量转义（console esc/textContent 双保险）、DisallowUnknownFields、只读闸门构造期强制、webhook Token、默认回环绑定、响应体上限。无注入/泄漏路径。G3 补上响应膨胀面。 |
| 性能 | **良**。F2 分 map 消除 O(n²) 采集成本、F3 签名去重消除写放大；当前热点只剩 L1/L2，量级触发前不构成问题。 |
| 最佳实践 | **良+**。gofmt/vet 零警告、确定性契约（有序输出/可重放）贯穿、幂等语义成体系（upsert/回滚/重建）。扣分：G1 暴露"语义注释无测试背书"的盲区。 |

## 三、整体质量评估与修复顺序

**总体评级：A-（上一轮 A-，本轮修复后同级的含金量更高——代码量 +40% 而实质缺陷数下降）**

代码库在 W4+W5 大规模扩张（新增约 2700 行）后仍保持架构纪律零破例，这是最重要的健康信号。本轮 3 项实质问题全部集中在**新增的持久化/时间处理边界**，均为第四轮引入或放大的回归面，说明"新代码需要一轮独立异常路径审查"的节奏是对的。

**修复顺序**（1~3 本轮已完成并推送）：
1. ~~G1 落库签名回滚~~ ✅（数据一致性，最高优先级）
2. ~~G2 零值 StartsAt 兜底~~ ✅（告警静默消失）
3. ~~G3 clusters 上限~~ ✅（资源保护）
4. L1 pipeline / L2 union-find —— 待簇数或拓扑规模触发（W6 评估前复看）
5. L5 console 竞态 / L8 CI race 确认 —— W6 开始前处理
