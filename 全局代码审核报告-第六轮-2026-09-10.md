# OpsCopilot 全局审核报告（第六轮 · 2026-09-10）

> 审核范围：commit `5aa935e` 全库（14 包）。覆盖第五轮后新增代码：W6-0 故障注入器、W6-1 pgx 数据链、M2 三模块骨架（incident/rca/notify + 迁移 000004）、控制台 UI 两版重制、run_demo.sh。
> 方法：门禁基线（build/vet/test/gofmt）+ 新代码精读 + 仓库卫生检查 + 依赖面复核。
> 上轮（R5）闭环 3 项：G1 落库签名回滚 / G2 零值 StartsAt / G3 列表上限。

---

## 一、发现的问题与风险点

### 当场已修复

**R6-1（中）· 测试文件漏交仓库**
- 位置：`cmd/opscopilot/faultinjection_test.go`
- 现象：W6-0 提交时该文件未被 git add——故障注入剧本聚合的 4 项集成测试只存在于本机，CI 与远端仓库没有这层覆盖。与 G1 教训同源：**测试不进仓库等于没写**。
- 修复：补交入库。

**R6-2（中）· 仓库卫生：构建产物残留 + gitignore 覆盖缺口**
- 现象：根目录残留 `console_smoke.exe`（safe-delete 失败遗留）与 `opscopilot.exe`（来源不明构建产物）；`.demo/`（run_demo 产物）、`*.log`、`evaluation-report.md` 未被 gitignore 覆盖；另有三轮历史审核报告（第三轮/复审/扫描）从未入库。
- 修复：gitignore 补 `.demo/`、`*.log`、`evaluation-report.md`、`csmoke.log`；删除残留 exe（确认无进程占用）；历史报告全部补交——审计文档必须在仓库里可追溯。

**R6-3（低，预防性）· M2 骨架两处实现契约未固化**
- `rca.ErrNotImplemented`：Pipeline 靠 `errors.Is` 识别占位步骤；W10 实现者若误用此哨兵表示真实失败、或不按 %w 包装错误，判定会静默漂移。
- `notify.Notifier`：`Gate.Admit` 同步调用 Send——W10 实渠道（HTTP webhook 类）若不带超时，阻塞会拖垮告警处理路径。
- 修复：两处接口注释固化强制契约（%w 包装约定 / 自带超时 ≤5s / 不得 panic）。

### 记录在案（暂不改代码，按计划排期）

| # | 位置 | 风险 | 建议与排期 |
|---|---|---|---|
| R6-4 | `internal/incident`（内存 Store） | 事件数据**重启即丢**，且 REST 已对外暴露 `/api/v1/incidents`——用户视角"写进去了"重启没了 | W9 事件 DB 后端落地前，在 incidents 响应加 `persistence:"memory"` 警示字段；W9 落 DB 后消除 |
| R6-5 | `cmd/opscopilot/noise_store_pg.go` | PG 写路径无语句超时（pgxpool 默认无 statement timeout）——DB 慢会拖长 ProcessAlerts 批处理（虽在锁外，但会堆积） | W9 统一给 DB 写路径加 `context.WithTimeout(5s)`；Redis 已有 go-redis 默认 3s |
| R6-6 | `internal/notify` Gate | 拦截计数只进内存——转正后重启归零，降噪效果不可追溯 | W10 转正时 Gate 计数随 verdict 落库自然持久化，无需单独存储 |
| R6-7 | `tools/evaluate.py` | 全量拉取 verdicts 无分页/窗口裁剪——7 天 × 高频告警后评估耗时增长 | W6-3 正式评估前加时间窗过滤（SQL WHERE occurred_at > now()-7d，已在报告侧有 horizon 但 SQL 侧无） |
| R6-8 | `cmd/noise.go` toEvent | G2 兜底用 `time.Now()`，同批告警毫秒级时间差理论上可跨聚类窗口桶边界拆分同指纹簇 | 概率极低；观察 verdict 分布，若出现再加"批内统一时间"策略 |
| R6-9 | 依赖面 | pgx v5.11.0（新引入）+ 传递依赖 golang.org/x/text 升至 0.29——go.sum 已锁，但无 CVE 扫描机制 | W7 私有化打包前引入 govulncheck 进 CI |

### 架构与质量面（无新问题）

- 边界纪律：M2 三模块零违例（incident 只持 cluster_key 字符串引用）。
- 安全面：REST incidents 只读无写路径；PG 全参数化查询；console 转义完整；faultinjector/evaluate.py 无注入面。
- 迁移：000004 up/down 成对、打库实测通过、CHECK 约束对齐状态机。
- 测试：M2 骨架即带测试（状态机/关联/流水线/闸门/REST），新增后 14 包全绿。

## 二、整体质量评估

**总体评级：A-（维持）**。本轮无高优先级缺陷——R5 的三项修复经受住了 W6/M2 两批新代码的交叉验证（G1 签名回滚在 multiRecordSink 双写下语义仍正确，G2 兜底在评估实测中生效）。新问题集中在**仓库卫生**（R6-1/2，流程问题而非代码问题）和**W9/W10 的预防性契约**（R6-3/4/5）——说明骨架先行策略下，风险正在从"代码缺陷"转向"实现契约的执行力"，这是健康的信号。

## 三、后续改进计划（按优先级）

| 优先级 | 任务 | 目标 | 涉及范围 | 预期效果 |
|---|---|---|---|---|
| **P0** | W6-2 挂机持续积累（进行中） | 7 天影子数据满窗 | 运行时（不改编译产物） | W6-3 评估的数据基础 |
| **P0** | W6-3 评估执行 | 准确率报告 >85%（M1 出口） | evaluate.py + alert_event/answerbook | M1 出口标准客观打分 |
| **P1** | W9 事件域落地 | F-01/02 转 DB 持久化 + 控制台事件页 | incident 包 + 迁移 + REST + console | 消除 R6-4（重启丢事件）；告警→簇→事件动线闭环 |
| **P1** | W10 通知转正 | F-11 实渠道（IM/webhook）+ Gate 接入告警链路 | notify 包 + main | 消除 R6-5/6；降噪真正开始"省事" |
| **P1** | W10 RCA 算法填充 | F-05/06 六步实现（as_of 取证/变更关联假设/归因） | rca 包 + REST 暴露 | 骨架变能力；契约已固化（R6-3） |
| **P2** | CI 加固 | govulncheck + -race job 确认覆盖 M2 并发路径 | CI 配置 | 消除 R6-9 + R5-L8 |
| **P2** | W7-1 单机打包 | compose 优化 + 安装脚本 + 4C8G 预检 | docker-compose/scripts | 私有化交付物 |
| **P3** | F-09/10 控制台增强、F-12 Runbook | 原型体验对齐 | console/新模块 | 功能点清单余项 |

**执行顺序**：挂机积累（日历时间）∥ W9 事件域 DB 后端 → W6-3 评估 → W10 转正 + RCA → W7 打包 → W8 验收。P0 依赖日历时间，代码工作优先填 P1 两项。
